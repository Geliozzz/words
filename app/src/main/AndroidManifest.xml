<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="oxbao.ru.words" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AddWordActivity"></activity>
        <activity android:name=".EditDBActivity"></activity>
        <activity android:name=".DeleteDBActivity"></activity>
        <activity android:name=".UpdateWordActivity"></activity>
        <activity android:name=".LearnActivity"></activity>
    </application>

</manifest>
